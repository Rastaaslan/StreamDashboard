import{it,expect,vi}from'vitest';import{DamPlannerClient}from'../integrations/damplanner/src/client.js';it('isole une panne HTTP après retry borné',async()=>{const fetcher=vi.fn(()=>Promise.resolve(new Response('',{status:503}))),c=new DamPlannerClient('http://x',fetcher as never);await expect(c.calendar()).rejects.toThrow('503');expect(fetcher).toHaveBeenCalledTimes(3)});it('récupère après une panne transitoire',async()=>{const payload={rows:[],warnings:[],fetchedAt:1,fromCache:false,items:[]},f=vi.fn().mockRejectedValueOnce(Error('offline')).mockResolvedValue(new Response(JSON.stringify(payload),{status:200}));await expect(new DamPlannerClient('http://x',f).calendar()).resolves.toEqual(payload)})
