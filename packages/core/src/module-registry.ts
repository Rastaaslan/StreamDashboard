import type { ModuleId, ProductProfile } from './product-profile.js';
export interface ModuleDefinition { id: ModuleId; label: string; availability: 'available' | 'unavailable'; dependencies: ModuleId[]; capabilities: string[] }
export const moduleRegistry: readonly ModuleDefinition[] = [
  ['obs','OBS',[],['scenes','audio','stream']], ['twitch','Twitch',[],['chat','audience','clips']], ['planning','Planning',[],['events']], ['notes','Notes',[],['notes']], ['checklist','Avant le live',[],['checklist']], ['templates','Modèles de live',[],['templates']], ['automations','Automatisations',[],['triggers','actions']], ['soundboard','Sons',['obs'],['play','stop','volume']], ['streamerPings','Alertes viewers',['twitch'],['rewards']], ['googleCalendar','Google Calendar',['planning'],['calendar']], ['discord','Discord',[],['account','bot','webhook']], ['streamlabs','Streamlabs',[],['donations']], ['wizebot','WizeBot',[],['events']],
].map(([id,label,dependencies,capabilities]) => ({ id: id as ModuleId, label: label as string, dependencies: dependencies as ModuleId[], capabilities: capabilities as string[], availability: 'available' as const }));
export function resolveModules(profile: ProductProfile) { return moduleRegistry.map(module => ({ ...module, enabled: profile.modules[module.id], blockedBy: module.dependencies.filter(id => !profile.modules[id]) })); }
export function setModule(profile: ProductProfile, id: ModuleId, enabled: boolean): ProductProfile { const next = structuredClone(profile); if (enabled) for (const dependency of moduleRegistry.find(item => item.id === id)?.dependencies ?? []) next.modules[dependency] = true; next.modules[id] = enabled; if (!enabled) for (const module of moduleRegistry) if (module.dependencies.includes(id)) next.modules[module.id] = false; return next; }
