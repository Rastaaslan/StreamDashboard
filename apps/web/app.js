const pages=[['overview','Vue d’ensemble','⌂'],['prepare','Préparer','✓'],['live','Live','●'],['planning','Planning','▣'],['deck','Control Deck','⌘'],['fun','Fun Deck','✦'],['diagnostics','Diagnostics','◫'],['settings','Réglages','⚙']];
let page='overview',state;const pending=new Set();
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
async function request(url,method='GET',body){const response=await fetch(url,{method,headers:{'content-type':'application/json'},body:body?JSON.stringify(body):undefined});const value=await response.json();if(!response.ok)throw Error(value.error?.message||value.error||`HTTP ${response.status}`);return value}
const messages={'checklist.toggle':'Checklist mise à jour','checklist.reset':'Checklist réinitialisée','timer.start':'Timer démarré','timer.pause':'Timer en pause','timer.reset':'Timer réinitialisé','timer.add':'Temps ajouté','session.start':'Diffusion démarrée','session.stop':'Diffusion arrêtée','obs.scene':'Scène OBS activée','obs.mute':'Audio OBS mis à jour','obs.volume':'Volume OBS mis à jour','mode.set':'Mode et scène OBS activés'};async function command(type,details={}){const key=type+JSON.stringify(details);if(pending.has(key))return;pending.add(key);try{const result=await request('/api/v1/commands','POST',{type,...details});applyStateUpdate(result.state);toast(messages[type]||'Commande exécutée');return result.state}catch(e){if(e.message.includes('Certaines vérifications')&&type==='session.start'&&confirm(e.message+'\n\nLancer quand même ?'))return command(type,{...details,force:true});toast(e.message,true);return undefined}finally{pending.delete(key)}}
async function refresh(){applyStateUpdate(await request('/api/v1/state'),true)}
function toast(text,bad=false){const el=$('#toast');el.textContent=text;el.className=bad?'show bad':'show';setTimeout(()=>el.className='',2200)}
function duration(seconds){const s=Math.max(0,seconds||0);return`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`}
function date(value){return new Date(value).toLocaleString('fr-FR',{dateStyle:'medium',timeStyle:'short'})}
function card(label,value,sub='',wide=''){return`<article class="card ${wide}"><span class="label">${label}</span><strong class="metric">${value}</strong><small>${sub}</small></article>`}
function nav(){$('nav').innerHTML=pages.map(([id,label,icon])=>`<button data-page="${id}" class="${id===page?'active':''}"><span>${icon}</span>${label}</button>`).join('');document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>window.go(b.dataset.page))}
function overview(){const next=state.nextLive,done=state.checklist.filter(x=>x.done).length;return`<div class="hero"><div><span class="kicker">BONJOUR, ${esc(state.settings.streamerName).toUpperCase()}</span><h2>${state.obs.streaming?'Le direct est en cours.':'Prêt à lancer votre prochain live ?'}</h2><p>${next?`${esc(next.title)} · ${date(next.startAtUtc)}`:'Ajoutez votre prochain live au planning.'}</p></div><button class="primary big" data-action="go" data-value="prepare">Préparer le live <b>→</b></button></div><div class="grid stats">${card('OBS',state.obs.connected?'Connecté':'Hors ligne',state.obs.scene||state.obs.error||'Mode autonome')}${card('Diffusion',state.obs.streaming?'EN DIRECT':'HORS LIGNE',state.obs.recording?'Enregistrement actif':'Aucun enregistrement')}${card('Préparation',`${done}/${state.checklist.length}`,done===state.checklist.length?'Tout est prêt':'Éléments validés')}${card('Timer',duration(timerRemaining()),state.timer.running?'Compte à rebours actif':'En attente')}</div><div class="section-head"><div><span class="label">ACCÈS RAPIDE</span><h3>Piloter sans quitter le cockpit</h3></div></div><div class="quick"><button data-action="go" data-value="live"><i>●</i><b>Console Live</b><small>Diffusion, timer et séquences</small></button><button data-action="go" data-value="deck"><i>⌘</i><b>Control Deck</b><small>Scènes et audio OBS</small></button><button data-action="go" data-value="planning"><i>▣</i><b>Planning</b><small>Organiser les prochains lives</small></button></div>`}
function prepare(){const done=state.checklist.filter(x=>x.done).length;return`<div class="panel"><div class="section-head"><div><span class="label">CHECKLIST AVANT LIVE</span><h3 id="check-count">${done}/${state.checklist.length} vérifications terminées</h3></div><button class="ghost compact" data-command="checklist.reset">Réinitialiser</button></div><div class="progress"><i id="check-progress" style="width:${done/state.checklist.length*100}%"></i></div><div class="checklist">${state.checklist.map(x=>`<button class="check ${x.done?'done':''}" data-check-id="${esc(x.id)}" data-command="checklist.toggle" data-value="${encodeURIComponent(x.id)}"><i>${x.done?'✓':''}</i><span><b>${esc(x.label)}</b><small>${x.done?'Validé':'À vérifier'}</small></span></button>`).join('')}</div><div class="launch"><div><b>${done===state.checklist.length?'Tout est prêt':'Préparation incomplète'}</b><small>${state.obs.connected?'OBS répond correctement.':esc(state.obs.error||'OBS est hors ligne : connectez-le avant de diffuser.')}</small></div><button class="primary" ${!state.obs.connected?'disabled':''} data-command="session.start">Démarrer le live</button></div></div>`}
function live(){return`<div class="live-console"><div class="timer-block"><span class="label">TIMER DE SESSION</span><strong id="timer-value">${duration(timerRemaining())}</strong><div class="button-row"><button class="primary" data-command="${state.timer.running?'timer.pause':'timer.start'}">${state.timer.running?'Pause':'Démarrer'}</button><button class="ghost" data-command="timer.add" data-seconds="300">+ 5 min</button><button class="ghost" data-command="timer.reset">Reset</button></div></div><div class="panel"><span class="label">ÉTAT DU DIRECT</span><h3>${state.obs.streaming?'Vous êtes en direct':'Diffusion arrêtée'}</h3><p class="muted">Scène active : <b>${esc(state.obs.scene||'Aucune')}</b></p><div class="mode-row">${['intro','live','pause','end'].map(x=>`<button class="mode ${state.mode===x?'active':''}" data-command="mode.set" data-value="${x}">${x}</button>`).join('')}</div><button class="${state.obs.streaming?'danger':'primary'} full-button" data-command="${state.obs.streaming?'session.stop':'session.start'}">${state.obs.streaming?'Arrêter la diffusion':'Démarrer le live'}</button></div></div>`}
function planning(){return`<div class="section-head"><div><span class="label">PLANNING SYNCHRONISÉ</span><h3>Prochains rendez-vous</h3><small class="muted">${state.twitch.connected?`Twitch · ${esc(state.twitch.displayName)}${state.twitch.lastSyncedAt?` · synchro ${date(state.twitch.lastSyncedAt)}`:''}`:'Connectez Twitch dans les réglages pour synchroniser.'}</small></div><div class="button-row">${state.twitch.connected?'<button class="ghost compact" data-action="sync-twitch">↻ Synchroniser Twitch</button>':''}<button class="primary compact" data-action="open-event">+ Ajouter</button></div></div><div class="schedule">${state.planning.sort((a,b)=>Date.parse(a.startAtUtc)-Date.parse(b.startAtUtc)).map(x=>`<article><time><b>${new Date(x.startAtUtc).toLocaleDateString('fr-FR',{day:'2-digit'})}</b>${new Date(x.startAtUtc).toLocaleDateString('fr-FR',{month:'short'})}</time><div><span class="tag">${x.source==='TWITCH'?'TWITCH · ':''}${x.category}</span><h3>${esc(x.title)}</h3><p>${date(x.startAtUtc)} — ${date(x.endAtUtc)}</p></div><button class="icon-btn" data-action="remove-event" data-value="${encodeURIComponent(x.id)}">×</button></article>`).join('')||'<div class="empty"><b>Aucun événement planifié</b><p>Votre planning autonome est prêt à accueillir un premier live.</p></div>'}</div><dialog id="event-dialog"><form id="event-form"><div class="section-head"><h3>Nouveau rendez-vous</h3><button type="button" class="icon-btn" data-action="close-dialog">×</button></div><label>Titre<input name="title" required></label><div class="form-grid"><label>Début<input name="start" type="datetime-local" required></label><label>Fin<input name="end" type="datetime-local" required></label></div><label>Type<select name="category"><option value="live">Live</option><option value="production">Production</option><option value="personal">Personnel</option></select></label><button class="primary" type="submit">Enregistrer</button></form></dialog>`}
function deck(){const o=state.obs,inputs=Object.entries(o.inputs).filter(([name])=>o.activeAudioInputs.includes(name));return`<div class="section-head"><div><span class="label">OBS WEBSOCKET</span><h3>Scènes</h3></div><span class="status ${o.connected?'ok':''}">${o.connected?'Connecté':'Indisponible'}</span></div><div class="deck">${o.scenes.map(x=>`<button class="pad ${o.scene===x?'active':''}" data-command="obs.scene" data-value="${encodeURIComponent(x)}"><i>▣</i><b>${esc(x)}</b></button>`).join('')||`<div class="empty">${esc(o.error||'Lancez OBS pour charger vos scènes.')}</div>`}</div><div class="section-head space"><div><h3>Mixeur audio contextuel</h3><small class="muted">Sources actives dans « ${esc(o.scene||'aucune scène')} » et périphériques audio globaux.</small></div><span class="status ${inputs.length?'ok':''}">${inputs.length}/${Object.keys(o.inputs).length} actives</span></div><div class="mixer">${inputs.map(([name,input])=>`<article><button class="mute ${input.muted?'muted':''}" data-command="obs.mute" data-value="${encodeURIComponent(name)}" data-muted="${!input.muted}">◉</button><div><b>${esc(name)}</b><input type="range" min="0" max="1.5" step=".05" value="${input.volume}" data-command="obs.volume" data-value="${encodeURIComponent(name)}"></div><span>${Math.round(input.volume*100)}%</span></article>`).join('')||'<p class="muted">Aucune source audio active dans cette scène.</p>'}</div>`}
function fun(){const media=state.obs.mediaInputs||[];return`<div class="section-head"><div><span class="label">FUN DECK · OBS</span><h3>Médias instantanés</h3><small class="muted">Relance réellement vos sources Média, VLC et Diaporama configurées dans OBS.</small></div><span class="status ${state.obs.connected?'ok':''}">${state.obs.connected?'OBS connecté':'OBS indisponible'}</span></div><div class="deck fun">${media.map(name=>`<button class="pad" data-command="obs.media.restart" data-value="${encodeURIComponent(name)}"><i>▶</i><b>${esc(name)}</b><small>Relancer la source OBS</small></button>`).join('')||'<div class="empty"><b>Aucun média OBS détecté</b><p>Ajoutez une source Média, VLC ou Diaporama dans OBS pour créer automatiquement un bouton utilisable.</p></div>'}</div>`}
function diagnostics(){const r=state.runtime;return`<div class="grid">${Object.entries(state.health).map(([name,h])=>card(name,`<span class="${h.ok?'green':'red'}">${h.ok?'Opérationnel':'Attention'}</span>`,esc(h.detail))).join('')}</div><div class="panel space"><div class="section-head"><span class="label">INFORMATIONS TECHNIQUES</span>${window.streamDashboardDesktop?'<button class="ghost compact" data-action="open-logs">Ouvrir les logs</button>':''}</div><div class="details"><span>StreamDashboard <b>${esc(r.serverVersion)}</b></span><span>Electron <b>${esc(r.electronVersion||'mode serveur')}</b></span><span>Node embarqué <b>${esc(r.nodeVersion)}</b></span><span>Système <b>${esc(r.platform)}</b></span><span>Port local <b>${r.port}</b></span><span>Dernière mise à jour <b>${date(state.at)}</b></span><span>Reconnexions OBS <b>${state.health.obs.reconnects}</b></span><span>Dossier logs <b>${esc(r.logsPath||'console de développement')}</b></span></div></div>`}
function settings(){const obsStatus=state.obs.connected?`<div class="status ok">OBS connecté${state.obs.obsVersion?` · v${esc(state.obs.obsVersion)}`:''}${state.obs.websocketVersion?` · WebSocket ${esc(state.obs.websocketVersion)}`:''}</div>`:`<div class="status">${esc(state.obs.error||'OBS non connecté')}</div>`;return`<form class="panel settings" id="settings-form"><span class="label">PRÉFÉRENCES DU COCKPIT</span><label>Nom affiché<input name="streamerName" value="${esc(state.settings.streamerName)}"></label><label>Couleur d’accent<select name="accent"><option value="violet">Violet</option><option value="cyan">Cyan</option><option value="rose">Rose</option></select></label><label class="switch"><span><b>Confirmer l’arrêt du live</b><small>Évite les arrêts accidentels</small></span><input name="confirmStop" type="checkbox" ${state.settings.confirmStop?'checked':''}></label><span class="label section-label">CONNEXION OBS</span><label class="switch"><span><b>Lancer OBS avec StreamDashboard</b><small>Effectif au prochain démarrage de l’application Windows</small></span><input name="launchObs" type="checkbox" ${state.settings.launchObs?'checked':''}></label><label>Adresse OBS WebSocket<input name="obsUrl" value="${esc(state.settings.obsUrl)}"><small>Exemple : ws://127.0.0.1:4455</small></label><label>Mot de passe OBS<input name="obsPassword" type="password" autocomplete="new-password" placeholder="${state.settings.obsPasswordSet?'Mot de passe enregistré — laisser vide pour conserver':'Mot de passe WebSocket OBS'}"><small>Le mot de passe n’est jamais renvoyé à l’interface après sauvegarde.</small></label>${obsStatus}<span class="label section-label">SCÈNES PAR MODE</span><div class="form-grid">${['intro','live','pause','end'].map(mode=>`<label>Scène ${mode}<select name="scene-${mode}"><option value="">Non configurée</option>${state.obs.scenes.map(scene=>`<option value="${esc(scene)}" ${state.settings.modeScenes?.[mode]===scene?'selected':''}>${esc(scene)}</option>`).join('')}</select></label>`).join('')}</div><span class="label section-label">CONNEXION TWITCH</span><div class="status ${state.twitch.connected?'ok':''}">${state.twitch.connected?`Connecté en tant que ${esc(state.twitch.displayName)}`:esc(state.twitch.error||'Twitch non connecté')}</div>${state.twitch.deviceAuthorization?`<div class="device-code"><small>CODE TWITCH</small><strong>${esc(state.twitch.deviceAuthorization.userCode)}</strong><a class="ghost" target="_blank" rel="noopener" href="${esc(state.twitch.deviceAuthorization.verificationUri)}">Ouvrir Twitch</a><small>Validez ce code avant ${date(state.twitch.deviceAuthorization.expiresAt)}. La connexion sera détectée automatiquement.</small></div>`:''}<div class="button-row"><button class="ghost" type="button" data-action="test-obs">Tester OBS</button>${state.twitch.connected?'<button class="ghost" type="button" data-action="disconnect-twitch">Déconnecter Twitch</button>':'<button class="ghost" type="button" data-action="connect-twitch">Connecter Twitch</button>'}<button class="primary" type="submit">Enregistrer</button></div></form>`}
function timerRemaining(){return state?.timer.running&&state.timer.deadline?Math.max(0,Math.ceil((state.timer.deadline-Date.now())/1000)):state?.timer.remaining||0}function render(){if(!state)return;document.documentElement.dataset.accent=state.settings.accent;$('#title').textContent=pages.find(x=>x[0]===page)[1];$('#obs-pill').textContent=`OBS ${state.obs.connected?'CONNECTÉ':'HORS LIGNE'}`;$('#obs-pill').className=`obs-pill ${state.obs.connected?'ok':''}`;$('#live-pill').textContent=state.obs.streaming?'● EN DIRECT':'HORS LIGNE';$('#live-pill').className=`live-pill ${state.obs.streaming?'on':''}`;const views={overview,prepare,live,planning,deck,fun,diagnostics,settings};$('#view').innerHTML=views[page]();bindForms()}

function applyStateUpdate(next,force=false){const previous=state;state=next;if(!previous||force){render();return}document.documentElement.dataset.accent=state.settings.accent;$('#obs-pill').textContent=`OBS ${state.obs.connected?'CONNECTÉ':'HORS LIGNE'}`;$('#obs-pill').className=`obs-pill ${state.obs.connected?'ok':''}`;$('#live-pill').textContent=state.obs.streaming?'● EN DIRECT':'HORS LIGNE';$('#live-pill').className=`live-pill ${state.obs.streaming?'on':''}`;updateTimer();if(page==='prepare')updateChecklist();const structural=page==='overview'||page==='prepare'&&previous.obs.connected!==state.obs.connected||page==='deck'&&JSON.stringify([previous.obs.scenes,previous.obs.activeAudioInputs,previous.obs.inputs,previous.obs.scene])!==JSON.stringify([state.obs.scenes,state.obs.activeAudioInputs,state.obs.inputs,state.obs.scene])||page==='planning'&&JSON.stringify(previous.planning)!==JSON.stringify(state.planning)||page==='live'&&(previous.obs.streaming!==state.obs.streaming||previous.mode!==state.mode||previous.obs.scene!==state.obs.scene)||page==='fun'&&(previous.obs.connected!==state.obs.connected||JSON.stringify(previous.obs.mediaInputs)!==JSON.stringify(state.obs.mediaInputs))||page==='settings'&&(previous.obs.connected!==state.obs.connected||previous.twitch.connected!==state.twitch.connected);if(structural&&!document.activeElement?.closest('form'))render()}
function updateTimer(){const el=$('#timer-value');if(el)el.textContent=duration(timerRemaining());if(page==='overview'){const metric=document.querySelector('.stats .card:nth-child(4) .metric');if(metric)metric.textContent=duration(timerRemaining())}}
function updateChecklist(){const done=state.checklist.filter(x=>x.done).length;const count=$('#check-count'),bar=$('#check-progress');if(count)count.textContent=`${done}/${state.checklist.length} vérifications terminées`;if(bar)bar.style.width=`${done/state.checklist.length*100}%`;for(const item of state.checklist){const el=document.querySelector(`[data-check-id="${CSS.escape(item.id)}"]`);if(!el)continue;el.classList.toggle('done',item.done);el.querySelector('i').textContent=item.done?'✓':'';el.querySelector('small').textContent=item.done?'Validé':'À vérifier'}}
async function prepareLive(){page='prepare';nav();render();if(state.obs.connected){await command('session.prepare');return}if(!window.streamDashboardDesktop?.ensureObsRunning){toast('OBS n’est pas lancé. Le lancement automatique est disponible dans l’application Desktop.',true);return}toast('Lancement OBS…');const result=await window.streamDashboardDesktop.ensureObsRunning();toast(result.detail,!result.launched&&!/déjà lancé/i.test(result.detail));const deadline=Date.now()+20000;let wait=400;while(Date.now()<deadline){await new Promise(resolve=>setTimeout(resolve,wait));const next=await request('/api/v1/state');applyStateUpdate(next);if(next.obs.connected){await command('session.prepare');toast('OBS connecté · scènes chargées');return}wait=Math.min(2000,Math.round(wait*1.5))}toast('OBS a été lancé mais StreamDashboard n’arrive pas à se connecter au WebSocket.',true)}
function bindForms(){const ef=$('#event-form');if(ef)ef.onsubmit=async e=>{e.preventDefault();const f=new FormData(ef);try{state=await request('/api/planning','POST',{title:f.get('title'),startAtUtc:new Date(f.get('start')).toISOString(),endAtUtc:new Date(f.get('end')).toISOString(),category:f.get('category')});render()}catch(x){toast(x.message,true)}};const sf=$('#settings-form');if(sf){sf.elements.accent.value=state.settings.accent;sf.onsubmit=async e=>{e.preventDefault();const f=new FormData(sf);const payload={streamerName:f.get('streamerName'),accent:f.get('accent'),confirmStop:f.get('confirmStop')==='on',launchObs:f.get('launchObs')==='on',obsUrl:f.get('obsUrl'),modeScenes:Object.fromEntries(['intro','live','pause','end'].map(mode=>[mode,f.get(`scene-${mode}`)]).filter(([,scene])=>scene))};const password=f.get('obsPassword');if(password)payload.obsPassword=password;try{state=await request('/api/settings','PUT',payload);render();toast('Réglages enregistrés')}catch(x){toast(x.message,true)}}}}
window.command=command;window.go=id=>{if(id==='prepare')return prepareLive();page=id;nav();render()};window.openEvent=()=>$('#event-dialog').showModal();window.removeEvent=async id=>{state=await request(`/api/planning/${id}`,'DELETE');render()};window.stopStream=()=>{if(state.obs.streaming&&(!state.settings.confirmStop||confirm('Arrêter réellement la diffusion ?')))command('session.stop')};window.testObs=async()=>{const sf=$('#settings-form');if(!sf)return;const f=new FormData(sf);try{const result=await request('/api/obs/test','POST',{obsUrl:f.get('obsUrl'),obsPassword:f.get('obsPassword')});toast(`OBS connecté · v${result.obsVersion||'?'} · WebSocket ${result.websocketVersion||'?'}`)}catch(e){toast(e.message,true)}};
window.connectTwitch=async()=>{try{const result=await request('/api/twitch/device','POST');if(window.streamDashboardDesktop)await window.streamDashboardDesktop.openTwitchActivation(result.verificationUri);else window.open(result.verificationUri,'_blank','noopener');toast(`Code Twitch : ${result.userCode}`);await refresh()}catch(e){toast(e.message,true)}};window.disconnectTwitch=async()=>{state=await request('/api/twitch/disconnect','POST');render();toast('Twitch déconnecté')};window.syncTwitch=async()=>{try{toast('Synchronisation Twitch…');state=await request('/api/twitch/sync','POST');render();toast('Planning Twitch synchronisé')}catch(e){toast(e.message,true)}};

document.addEventListener('click',event=>{const el=event.target.closest('[data-action],[data-command]');if(!el)return;const value=el.dataset.value?decodeURIComponent(el.dataset.value):undefined;if(el.dataset.command){const type=el.dataset.command,details={};if(type==='session.stop'&&state.settings.confirmStop&&!confirm('Arrêter réellement la diffusion ?'))return;if(type==='checklist.toggle')details.id=value;if(type==='mode.set')details.mode=value;if(type==='timer.add')details.seconds=+el.dataset.seconds;if(type==='obs.scene')details.scene=value;if(type==='obs.mute'){details.input=value;details.muted=el.dataset.muted==='true'}if(type==='obs.stream')details.start=el.dataset.start==='true';if(type==='obs.media.restart')details.input=value;el.disabled=true;el.dataset.status='loading';void command(type,details).finally(()=>{if(el.isConnected){el.disabled=false;el.dataset.status='idle'}});return}const actions={go:()=>window.go(value),'stop-stream':window.stopStream,'sync-twitch':window.syncTwitch,'open-event':window.openEvent,'remove-event':()=>window.removeEvent(value),'close-dialog':()=>el.closest('dialog')?.close(),'open-logs':()=>window.streamDashboardDesktop?.openLogs(),'test-obs':window.testObs,'disconnect-twitch':window.disconnectTwitch,'connect-twitch':window.connectTwitch};void actions[el.dataset.action]?.()});
document.addEventListener('change',event=>{const el=event.target;if(el?.dataset?.command==='obs.volume')void command('obs.volume',{input:decodeURIComponent(el.dataset.value),volume:+el.value})});
function socket(){const ws=new WebSocket(`${location.protocol==='https:'?'wss':'ws'}://${location.host}/ws/v1`);ws.onopen=()=>{$('#socket').innerHTML='<i></i> Temps réel';$('#socket').classList.add('online')};ws.onmessage=e=>{const event=JSON.parse(e.data);if(event.type==='state.updated')applyStateUpdate(event.data)};ws.onclose=()=>{ $('#socket').textContent='Reconnexion…';setTimeout(socket,1500)}}
nav();await refresh();socket();setInterval(updateTimer,250);