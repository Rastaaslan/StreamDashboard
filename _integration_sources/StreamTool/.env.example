# Keep the default loopback bind unless LAN access is deliberately required.
HOST=127.0.0.1
PORT=8787
PROFILE=dam
OBS_WEBSOCKET_URL=ws://127.0.0.1:4455
OBS_WEBSOCKET_PASSWORD=
# Required for API commands when HOST is not a loopback address.
API_TOKEN=
